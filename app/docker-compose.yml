version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # Uncomment and adjust the following line if you keep an .env in the repo
    # env_file:
    #   - .env
